services:
  - type: web
    name: rpg-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn APIMisiones:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12